#include "monty.h"
/**
 * stack - sets the format of the data to a stack 
 * @stack: pointer to the stack
 * @line_number: line number where the stack function is called
 */
void stack(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	arguments->stack = 1;
}
